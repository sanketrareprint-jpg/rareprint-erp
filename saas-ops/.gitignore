# Real secrets — never commit.
.env

# The real customer registry contains database connection strings
# (passwords included) for every customer. Never commit it.
registry.json

node_modules/

# Local record of superadmin actions (impersonation etc). Operational
# history, machine-local — not committed.
audit-log.jsonl
